add-todo:
  handler: functions/add-todo/index.handler
  role: AddTodoRole
  environment:
    DISTRIBUTION_ID: !Ref CloudFrontDistribution
  events:
    - httpApi:
        method: POST
        path: /api/todo
        authorizer:
          name: Authorizer
        
